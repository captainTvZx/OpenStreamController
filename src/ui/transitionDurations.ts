/** Shared bounds for the transition-duration controls, in milliseconds. */
export const DURATION_MIN_MS = 50;
export const DURATION_MAX_MS = 3000;

/** Handy values so the common cases need no dragging. */
export const DURATION_PRESETS = [100, 300, 500, 1000, 2000];
